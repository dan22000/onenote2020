<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OneNote" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Title">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">44sp</item>
        <item name="android:text">@string/app_name</item>
    </style>

    <style name="Icon">
        <item name="android:src">@drawable/ic_event_note</item>
        <item name="tint">@color/black</item>
    </style>

    <style name="Button">
        <item name="android:layout_margin">@dimen/padding</item>
        <item name="android:text">@string/login</item>
        <item name="backgroundTint">@color/black</item>
    </style>

    <style name="Button.Save">
        <item name="android:text">@string/save</item>
    </style>
</resources>